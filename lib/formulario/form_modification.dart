import 'package:flutter/material.dart';

class modificar_formulario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Modificación de Formulario'),
      ),
      body: Center(
        child: Text('Formulario 883'),
      ),
    );
  }
}
