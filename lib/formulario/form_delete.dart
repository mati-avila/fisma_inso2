import 'package:flutter/material.dart';

class eliminar_formulario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eliminar de Formulario'),
      ),
      body: Center(
        child: Text('Formulario 883'),
      ),
    );
  }
}
